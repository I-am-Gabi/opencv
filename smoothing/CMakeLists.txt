cmake_minimum_required (VERSION 2.6)
project (opencv)
find_package (OpenCV REQUIRED)
include_directories (${OpenCV_INCLUDE_DIRS})
add_executable (opencv opencv.c)
target_link_libraries (opencv ${OpenCV_LIBS})